version: '3.8'

services:
  # app:
  #   build: .
  #   depends_on:
  #     - db
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     - MYSQL_HOST=db
  #     - MYSQL_USER=UserVersanetDb
  #     - MYSQL_PASSWORD=versanetdb$2024
  #     - MYSQL_DATABASE=versanetbd
  #     - MYSQL_PORT=3306
  #     - JWT_SECRET=mysecretkey
  #     - PORT=5000
  #     - MAIL_HOST=smtp.gmail.com
  #     - MAIL_PORT=465
  #     - MAIL_USER=yancarojas2003@gmail.com
  #     - MAIL_PASS=bokk vora jypi yxlh
  #     - MAIL_SECURE=true
  #     - MAIL_FROM=Versanet
  #   volumes:
  #     - .:/usr/src/app

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: versanetbd
      MYSQL_USER: UserVersanetDb
      MYSQL_PASSWORD: versanetdb$2024
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      # - ./src/infrastructure/db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro

volumes:
  db_data:
