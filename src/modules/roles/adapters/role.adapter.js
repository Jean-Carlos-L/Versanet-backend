export function roleAdapterDTO(rol) {
    return {
        id: rol.id,
        name: rol.name
    }
}

export const roleAdapterEntity = (rol) => {
    return {
        id: rol.id,
        name: rol.name
    }
}